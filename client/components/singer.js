import React from "react";
import PropTypes from "prop-types";
import * as Tone from "tone";

const Singer = ({ noteList }) => {
  return <div></div>;
};

Singer.propTypes = {
  noteList: PropTypes.array.isRequired,
};

export default Singer;
